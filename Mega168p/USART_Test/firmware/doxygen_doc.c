/**
   @mainpage

   @par What?
   AVR(ATMEga168p)でUART(USARTモジュール)を使うためのライブラリ<br>
   @par
   他のAVRでもレジスタとビットのマクロを変えれば多分動く（ように書いたつもり）。他のAVRで使ったら書き加えていく予定
   とりあえず8bitデータ長、ストップビット1bit、非同期に設定されている。ボーレートはマクロで変更可能


 */
